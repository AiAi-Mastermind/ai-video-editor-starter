# Start Here

Paste this first:

```
Read AGENTS.md. Then run the desk check and tell me in plain English what is connected and what is not. Do not make any video yet.
```

After setup, run the sample. It uses `brand/sample-brand-kit.md`, so nothing needs filling in. Paste this:

```
Run assignment 1 with the sample brief.
```

To make the videos yours, paste this:

```
Interview me for the brand kit.
```

When you have a logo, you can drop it into `brand/assets/`. A logo is optional for the sample run.
