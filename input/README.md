# Input

Put source recordings in `input/recordings/`. A good file name is `monday-recording.mp4`. Do not use a client or customer name in a file name.

Recordings in that folder are ignored by git, the file tracking tool, so they do not get uploaded with this project. Keep your own backup.
